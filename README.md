# language-detector-app
A simple Streamlit-based language detection web app that uses a pre-trained scikit-learn model to identify the language of a given text input. The app returns both the predicted language and a confidence score
